# Frontend Production Environment
# API Base URL for live server
# Uses relative path /api which will be proxied to the backend
# This works because the live server has Nginx/reverse proxy configured to forward /api to the backend
VITE_API_BASE_URL=/api
